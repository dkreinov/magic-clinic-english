# Runtime LLM + TTS key, server-side only
OPENAI_API_KEY=
# Vercel Blob storage
BLOB_READ_WRITE_TOKEN=
# Dormant fallback — do not use at runtime
ANTHROPIC_API_KEY=
# Shared entry code for the API (keeps casual visitors out). Unset = gate open.
APP_CODE=
